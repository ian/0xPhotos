// import { algoliaIndex } from '../../../lib/algolia'
// import { getAssetNFT } from '../../../lib/covalent'

export default async function handler(req, res) {
  res.status(200).json({ res: 'hi' })
}
